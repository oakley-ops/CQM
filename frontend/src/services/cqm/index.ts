/**
 * CQM Services Index
 * Central export point for all CQM services
 */

export { facilityService } from './facilityService';
export { dashboardService } from './dashboardService';

// Note: Other services (testDefinition, testResult, audit, nonConformity, capa, cardBatch)
// will be created as needed. For now, facility and dashboard services are sufficient
// to get the app running.

